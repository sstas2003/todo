export const state = (state) => state.todoListReducer;
export const filters = (state) => state.todoListReducer.filters;
export const list = (state) => state.todoListReducer.list;
export const isLoading = (state) => state.todoListReducer.isLoading;
